source("renv/activate.R")
# Activa el entorno renv del proyecto si ya fue inicializado.
# (renv/activate.R lo genera setup/bootstrap.R en la primera puesta en marcha;
#  el 'if' evita que una sesión falle antes de esa inicialización.)
if (file.exists("renv/activate.R")) source("renv/activate.R")
